<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>Permanent Tree Growth</title>
    <style>
        body {
            background-color: #81d4fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: sans-serif;
            overflow: hidden;
        }

        .container {
            background: linear-gradient(to bottom, #4fc3f7 0%, #e1f5fe 100%);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.15);
            width: 500px;
            height: 500px;
            position: relative;
            overflow: hidden;
        }

        /* === ☁️ 云朵动画 (保持循环，因为云一直在飘) === */
        @keyframes drift {
            from { transform: translateX(-150px); }
            to { transform: translateX(600px); }
        }

        .cloud {
            fill: #ffffff;
            opacity: 0.8;
        }
        .cloud-1 { animation: drift 25s linear infinite; opacity: 0.6; transform: scale(0.8); }
        .cloud-2 { animation: drift 15s linear infinite; animation-delay: -5s; }
        .cloud-3 { animation: drift 30s linear infinite; animation-delay: -15s; opacity: 0.5; transform: scale(0.6); }

        /* === 🌳 树木生长动画 (关键修改：只播放一次) === */
        
        /* 1. 树枝生长：从无到有，播放完停在最后的状态 */
        @keyframes grow-branch {
            0% { stroke-dashoffset: 300; } 
            100% { stroke-dashoffset: 0; }
        }

        /* 2. 树叶绽放：从小到大，播放完停在最后 */
        @keyframes leaf-bloom {
            0% { transform: scale(0); opacity: 0; }
            80% { transform: scale(1.1); opacity: 1; }
            100% { transform: scale(1); opacity: 1; }
        }

        /* 3. 摇摆：永远循环，因为风一直吹 */
        @keyframes wind-sway {
            0%, 100% { transform: rotate(0deg); }
            50% { transform: rotate(2deg); }
        }

        /* === 样式配置 === */

        /* 树枝 */
        .branch-path {
            fill: none;
            stroke: #5d4037;
            stroke-linecap: round;
            stroke-dasharray: 300;
            stroke-dashoffset: 300; /* 初始隐藏 */
            
            /* 关键修改：forwards 让动画结束后停留在最后一帧(显示状态)，不加 infinite */
            animation: grow-branch 3s ease-out forwards; 
        }

        .trunk-main { animation-delay: 0.5s; stroke-width: 20; }
        .branch-left { animation-delay: 2.0s; stroke-width: 14; }
        .branch-right { animation-delay: 2.3s; stroke-width: 12; }
        .branch-top { animation-delay: 2.7s; stroke-width: 10; }

        /* 树叶 */
        .leaf-group circle {
            transform-origin: center;
            transform: scale(0); /* 初始隐藏 */
            /* 关键修改：只播放一次，forwards 保持显示 */
            animation: leaf-bloom 1s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
        }

        .leaf-1 { animation-delay: 3.5s !important; fill: #66bb6a; }
        .leaf-2 { animation-delay: 3.7s !important; fill: #81c784; }
        .leaf-3 { animation-delay: 4.0s !important; fill: #a5d6a7; }
        .leaf-4 { animation-delay: 4.3s !important; fill: #4caf50; }

        /* 整体摇摆容器 */
        .tree-sway-wrapper {
            transform-origin: bottom center;
            /* 摇摆动画依然是 infinite，因为大树长好后也会随风动 */
            animation: wind-sway 4s ease-in-out infinite; 
            /* 等树干开始长出来后再开始摇摆，避免画面一开始就晃动 */
            animation-delay: 0.5s; 
        }

        /* 太阳微动 */
        .sun {
            animation: sun-glow 4s ease-in-out infinite alternate;
        }
        @keyframes sun-glow {
            from { r: 35; opacity: 0.8; }
            to { r: 38; opacity: 1; }
        }

        /* 为了方便重看动画，加了一个刷新提示 */
        .refresh-hint {
            position: absolute;
            bottom: 10px;
            right: 15px;
            font-size: 12px;
            color: #fff;
            opacity: 0.6;
            cursor: pointer;
        }

    </style>
</head>
<body>

<div class="container" onclick="location.reload()">
    <svg viewBox="0 0 400 400" xmlns="http://www.w3.org/2000/svg">
        <defs>
            <linearGradient id="groundGrad" x1="0%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" stop-color="#9ccc65"/>
                <stop offset="100%" stop-color="#7cb342"/>
            </linearGradient>
            
            <g id="cloudShape">
                <circle cx="30" cy="30" r="20" />
                <circle cx="55" cy="30" r="25" />
                <circle cx="80" cy="30" r="20" />
                <circle cx="45" cy="15" r="20" />
                <circle cx="65" cy="15" r="18" />
            </g>
        </defs>

        <circle class="sun" cx="340" cy="60" r="35" fill="#fff176" />

        <use href="#cloudShape" class="cloud cloud-3" y="40" />
        <use href="#cloudShape" class="cloud cloud-1" y="80" />
        <use href="#cloudShape" class="cloud cloud-2" y="20" transform="scale(1.2)" />

        <path d="M-50,360 Q200,340 450,360 L450,450 L-50,450 Z" fill="url(#groundGrad)" />

        <g class="tree-sway-wrapper">
            <!-- 树干 -->
            <path class="branch-path trunk-main" d="M200,380 Q200,300 200,240" />
            <path class="branch-path branch-left" d="M200,280 Q150,230 130,180" />
            <path class="branch-path branch-right" d="M200,260 Q250,210 280,170" />
            <path class="branch-path branch-top" d="M200,240 Q200,180 190,120" />

            <!-- 树叶 -->
            <g class="leaf-group">
                <circle class="leaf-1" cx="120" cy="170" r="45" />
                <circle class="leaf-2" cx="150" cy="190" r="35" />
                <circle class="leaf-4" cx="190" cy="110" r="50" />
                <circle class="leaf-3" cx="220" cy="130" r="40" />
                <circle class="leaf-1" cx="290" cy="160" r="45" />
                <circle class="leaf-4" cx="260" cy="180" r="38" />
                <circle class="leaf-2" cx="200" cy="180" r="55" opacity="0.9"/>
            </g>
        </g>
    </svg>
    <div class="refresh-hint">点击画面重新生长 ↻</div>
</div>

</body>
</html>