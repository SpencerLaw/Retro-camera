<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>Just A Cute Heart</title>
    <style>
        body {
            /* 纯白背景 */
            background-color: #ffffff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* 心跳动画：像果冻一样Q弹 */
        .heart-bounce {
            animation: heartbeat 1.5s ease-in-out infinite;
            transform-origin: center;
            cursor: pointer; /* 鼠标放上去变小手 */
        }

        @keyframes heartbeat {
            0% { transform: scale(1); }
            14% { transform: scale(1.1); }
            28% { transform: scale(1); }
            42% { transform: scale(1.1); }
            70% { transform: scale(1); }
        }
    </style>
</head>
<body>

    <svg width="300" height="300" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">
        <defs>
            <!-- 甜甜的草莓粉渐变 -->
            <radialGradient id="strawberryPink" cx="30%" cy="30%" r="80%">
                <stop offset="0%" stop-color="#ffbfd3" /> <!-- 亮光处淡粉 -->
                <stop offset="60%" stop-color="#ff6b95" /> <!-- 主体粉 -->
                <stop offset="100%" stop-color="#ff3366" /> <!-- 边缘深粉 -->
            </radialGradient>
            
            <!-- 投影：让它浮在白纸上 -->
            <filter id="softShadow" x="-20%" y="-20%" width="140%" height="140%">
                <feDropShadow dx="0" dy="8" stdDeviation="5" flood-color="#ffb3c6" flood-opacity="0.5"/>
            </filter>
        </defs>

        <g class="heart-bounce">
            <!-- 1. 爱心主体 -->
            <!-- 加了 filter="url(#softShadow)" 让它有立体感 -->
            <path d="M100,175 
                     C 40,115 20,85 20,60 
                     C 20,25 50,15 75,15 
                     C 92,15 100,25 100,30 
                     C 100,25 108,15 125,15 
                     C 150,15 180,25 180,60 
                     C 180,85 160,115 100,175 Z" 
                  fill="url(#strawberryPink)" 
                  stroke="#ff3366" stroke-width="4" stroke-linejoin="round"
                  filter="url(#softShadow)" />

            <!-- 2. 卡通高光 (左上角) - 关键的可爱点 -->
            <ellipse cx="60" cy="50" rx="12" ry="20" fill="#ffffff" transform="rotate(-15 60 50)" opacity="0.9"/>
            <circle cx="82" cy="40" r="5" fill="#ffffff" opacity="0.8"/>

            <!-- 3. 右侧小反光 -->
            <path d="M150,50 q10,10 5,20" fill="none" stroke="#ffffff" stroke-width="4" stroke-linecap="round" opacity="0.5"/>
        </g>
    </svg>

</body>
</html>