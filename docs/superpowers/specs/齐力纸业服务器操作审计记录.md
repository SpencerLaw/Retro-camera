# 齐力纸业 VPS 操作审计记录

**记录日期**：二零二六年六月六日
**记录用途**：记录开发方通过 SSH 在客户 VPS 上执行过的操作，方便客户、项目负责人和后续维护人员审计。
**记录原则**：只记录操作目的、命令摘要、影响路径和执行结果，不记录服务器密码、密钥、数据库密码、`.env` 内容等敏感信息。

---

## 一、审计范围

本文件记录二零二六年六月六日对齐力纸业 VPS 执行的服务器操作。

本次操作目标：

1. 盘点服务器当前运行方式。
2. 备份旧网站源码。
3. 备份 Nginx 配置。
4. 处理旧站目录中的可疑文件。
5. 上传并部署新前端静态页面。
6. 切换 Nginx 到新静态站点。
7. 停止旧 Next.js 服务。
8. 验证正式域名访问状态。

本次没有执行的操作：

1. 没有读取 `.env` 文件内容。
2. 没有导出数据库。
3. 没有修改 MySQL 数据。
4. 没有删除旧网站源码目录。
5. 没有删除旧网站数据库。
6. 没有改动宝塔面板账号。
7. 没有改动服务器 root 密码。

---

## 二、操作摘要

| 时间 | 动作 | 影响对象 | 结果 |
| --- | --- | --- | --- |
| 2026-06-06 21:44-21:55 | VPS 只读盘点 | 系统、Nginx、MySQL、Node、站点目录 | 已确认旧站为 Next.js 项目 |
| 2026-06-06 22:03 | 备份旧站源码 | `/www/wwwroot/paper-main` | 已生成 94M 备份包 |
| 2026-06-06 22:03 | 备份 Nginx 配置 | `node_paper_main.conf` | 已生成配置备份 |
| 2026-06-06 22:03 | 隔离可疑文件 | `.pwned`、`sshddm` | 已复制到隔离目录 |
| 2026-06-06 22:03 | 删除线上可疑文件 | 旧站目录 | 已删除，复查不存在 |
| 2026-06-06 22:04 | 上传新前端静态包 | `/www/backup/qilipaper` | 上传成功 |
| 2026-06-06 22:04 | 解压新前端 | `/www/wwwroot/qilipaper-vanglam-static` | 解压成功 |
| 2026-06-06 22:05 | 修改 Nginx 配置 | `qilipaper.com` 站点配置 | 已切到静态目录 |
| 2026-06-06 22:06 | 修复 `/vanglam` 路由 | Nginx `try_files` | 已修复 403 问题 |
| 2026-06-06 22:06 | 停止旧 Next.js 服务 | `npm start`、`next-server` | 已停止 |
| 2026-06-06 22:07 | 验证正式域名 | `qilipaper.com` | 核心路径均返回 200 |
| 2026-06-06 22:10 | 复查安全处理状态 | 可疑文件、进程、Nginx、域名 | 复查通过 |
| 2026-06-06 23:29 | 最终安全复查 | 端口、服务、SSH、可疑文件 | 已确认剩余风险为 SSH 暴力扫描 |
| 2026-06-06 23:31 | 关闭默认 phpMyAdmin 入口 | Nginx 主配置 `listen 888` | 已备份配置、移除入口并重载 |
| 2026-06-06 23:31 | 删除隔离目录可疑文件副本 | `.pwned`、`sshddm` 隔离副本 | 已记录 SHA256 后删除 |

---

## 三、详细操作记录

### 1. 建立 SSH 连接

**目的**：确认开发方可以通过 SSH 进入 VPS，后续可直接执行盘点、备份和部署。

**命令摘要**：

```bash
ssh root@服务器地址 "hostname; whoami; pwd"
```

**结果**：

```text
hostname: iZrj9gpb2xsv0d9kt1jqtoZ
whoami: root
pwd: /root
```

**备注**：

1. 只使用 SSH 公钥登录。
2. 未在文档中记录 root 密码。
3. 未在文档中记录私钥内容。

---

### 2. 服务器基础盘点

**目的**：确认服务器系统、内存、磁盘、Web 服务和数据库状态。

**命令摘要**：

```bash
hostname
whoami
pwd
date
cat /etc/os-release
uptime
free -h
df -h
```

**确认结果**：

| 项目 | 结果 |
| --- | --- |
| 系统 | Alibaba Cloud Linux 3.2104 U11 |
| 当前用户 | root |
| 内存 | 约 1.8 GiB |
| Swap | 约 1.0 GiB |
| 磁盘 | 40G 云盘，剩余约 20G |
| 当前目录 | `/root` |

**影响范围**：只读查询，无文件变更。

---

### 3. 服务与端口盘点

**目的**：确认当前网站由什么服务承载。

**命令摘要**：

```bash
systemctl status nginx --no-pager -l
systemctl status mysqld --no-pager -l
systemctl status httpd --no-pager -l
systemctl status docker --no-pager -l
ss -tulpn
```

**确认结果**：

| 服务 | 状态 |
| --- | --- |
| Nginx | 运行中 |
| MySQL | 运行中 |
| Apache/httpd | 未安装 |
| Docker | 未安装 |
| 80 | Nginx 监听 |
| 443 | Nginx 监听 |
| 3000 | 旧 Next.js 服务监听 |
| 3306 | MySQL 监听 |
| 8888 | 宝塔面板监听 |

**影响范围**：只读查询，无文件变更。

---

### 4. 旧站目录与 Nginx 配置盘点

**目的**：确认域名绑定、站点目录和旧网站运行方式。

**命令摘要**：

```bash
ls -lah /www
ls -lah /www/wwwroot
ls -lah /www/server/panel/vhost/nginx
grep -R "server_name\|root \|proxy_pass\|try_files\|ssl_certificate" /www/server/panel/vhost/nginx
```

**确认结果**：

```text
旧站目录：/www/wwwroot/paper-main
Nginx 配置：/www/server/panel/vhost/nginx/node_paper_main.conf
域名：qilipaper.com
旧站代理：proxy_pass http://127.0.0.1:3000
```

**影响范围**：只读查询，无文件变更。

---

### 5. 旧 Next.js 进程盘点

**目的**：确认旧站是如何启动的，避免切换时误杀无关服务。

**命令摘要**：

```bash
ps -fp 旧进程号
tr '\0' ' ' < /proc/旧进程号/cmdline
readlink -f /proc/旧进程号/cwd
ps -eo pid,user,cmd | grep -E "next-server|next start|node|npm"
```

**确认结果**：

```text
父进程：www 用户运行 npm start
子进程：next-server v15.3.1
工作目录：/www/wwwroot/paper-main
```

**影响范围**：只读查询，无文件变更。

---

### 6. 发现可疑文件

**目的**：确认旧站目录是否存在异常文件。

**命令摘要**：

```bash
ls -lah /www/wwwroot/paper-main/.pwned /www/wwwroot/paper-main/sshddm
stat /www/wwwroot/paper-main/.pwned /www/wwwroot/paper-main/sshddm
file /www/wwwroot/paper-main/.pwned /www/wwwroot/paper-main/sshddm
sha256sum /www/wwwroot/paper-main/.pwned /www/wwwroot/paper-main/sshddm
ps -eo pid,user,cmd | grep -E "sshddm"
```

**确认结果**：

| 文件 | 判断 |
| --- | --- |
| `.pwned` | 非正常项目文件 |
| `sshddm` | Linux 64 位可执行文件，非正常项目文件 |

**重要记录**：

```text
.pwned SHA256: 1060092d1ce0ae5ca5ac11bc1d078c5fa9e263f3fb6c736293a5dbb018e59258
sshddm SHA256: d04380d79168f863515bb6c625de913a2f62e8160d6913c57b83a6cb6b36c5ed
```

**影响范围**：此步骤只读查询，没有删除文件。

---

### 7. 备份旧网站源码

**目的**：按照客户要求，将旧网站源码完整备份一份，后续需要时可查阅或回滚。

**命令摘要**：

```bash
mkdir -p /www/backup/qilipaper

cd /www/wwwroot

tar --warning=no-file-changed \
  --exclude='paper-main/node_modules' \
  --exclude='paper-main/.next' \
  --exclude='paper-main/.pwned' \
  --exclude='paper-main/sshddm' \
  -czf /www/backup/qilipaper/paper-main-source-20260606-220341.tar.gz \
  paper-main
```

**生成文件**：

```text
/www/backup/qilipaper/paper-main-source-20260606-220341.tar.gz
```

**文件大小**：

```text
约 94M
```

**备注**：

1. 备份排除了 `node_modules`。
2. 备份排除了 `.next`。
3. 备份排除了异常文件 `.pwned` 和 `sshddm`。
4. 旧网站原目录没有被删除。

---

### 8. 备份 Nginx 配置

**目的**：切换前保留 Nginx 配置备份，方便回滚。

**命令摘要**：

```bash
cp -a /www/server/panel/vhost/nginx/node_paper_main.conf \
  /www/backup/qilipaper/node_paper_main.conf.20260606-220341.bak
```

后续切换前和修复路由前也分别做了备份：

```text
/www/backup/qilipaper/node_paper_main.conf.before-static-20260606-220509.bak
/www/backup/qilipaper/node_paper_main.conf.fix-vanglam-route-20260606-220624.bak
```

**结果**：配置备份成功。

---

### 9. 隔离可疑文件

**目的**：删除异常文件前先保留隔离副本，方便后续安全排查。

**命令摘要**：

```bash
mkdir -p /www/backup/qilipaper/security-20260606-220341
cp -a /www/wwwroot/paper-main/.pwned /www/backup/qilipaper/security-20260606-220341/.pwned
cp -a /www/wwwroot/paper-main/sshddm /www/backup/qilipaper/security-20260606-220341/sshddm
chmod 600 /www/backup/qilipaper/security-20260606-220341/.pwned
chmod 600 /www/backup/qilipaper/security-20260606-220341/sshddm
```

**隔离目录**：

```text
/www/backup/qilipaper/security-20260606-220341
```

**隔离文件权限**：

```text
.pwned 600
sshddm 600
```

**备注**：

1. 隔离文件保留用于查证。
2. 隔离文件已取消执行权限。
3. 隔离目录不作为网站访问目录。

---

### 10. 删除旧站目录中的可疑文件

**目的**：从线上旧站目录删除异常文件，降低风险。

**删除前检查**：

```bash
ps -eo pid,user,cmd | grep -E "/www/wwwroot/paper-main/sshddm|sshddm" | grep -v grep
```

**检查结果**：

```text
未发现 sshddm 正在运行的进程
```

**命令摘要**：

```bash
rm -f /www/wwwroot/paper-main/.pwned /www/wwwroot/paper-main/sshddm
```

**删除后复查**：

```bash
ls -lah /www/wwwroot/paper-main/.pwned /www/wwwroot/paper-main/sshddm
```

**复查结果**：

```text
旧站目录中的 .pwned 已删除
旧站目录中的 sshddm 已删除
```

---

### 11. 上传新前端静态部署包

**目的**：把本地构建好的新前端部署包上传到 VPS。

**本地包**：

```text
D:\webcode\Retro-camera-main\.codex-tmp\qilipaper-vanglam-dist-20260605.zip
```

**服务器保存位置**：

```text
/www/backup/qilipaper/qilipaper-vanglam-dist-20260605.zip
```

**命令摘要**：

```bash
scp qilipaper-vanglam-dist-20260605.zip root@服务器:/www/backup/qilipaper/
```

**结果**：上传成功。

---

### 12. 解压新前端到干净目录

**目的**：不复用旧站目录，使用全新目录承载静态前端。

**命令摘要**：

```bash
mkdir -p /www/wwwroot/qilipaper-vanglam-static.tmp
unzip -oq /www/backup/qilipaper/qilipaper-vanglam-dist-20260605.zip \
  -d /www/wwwroot/qilipaper-vanglam-static.tmp

mv /www/wwwroot/qilipaper-vanglam-static.tmp \
  /www/wwwroot/qilipaper-vanglam-static

chown -R www:www /www/wwwroot/qilipaper-vanglam-static
find /www/wwwroot/qilipaper-vanglam-static -type d -exec chmod 755 {} \;
find /www/wwwroot/qilipaper-vanglam-static -type f -exec chmod 644 {} \;
```

**新站目录**：

```text
/www/wwwroot/qilipaper-vanglam-static
```

**目录内容**：

```text
index.html
assets/
vanglam/
```

**结果**：解压成功，权限已设置。

---

### 13. 切换 Nginx 到新静态站

**目的**：让 `qilipaper.com` 从旧 Next.js 反向代理切换为新静态前端。

**切换前备份**：

```text
/www/backup/qilipaper/node_paper_main.conf.before-static-20260606-220509.bak
```

**配置变更摘要**：

旧配置：

```nginx
location / {
    proxy_pass http://127.0.0.1:3000;
}
```

新配置：

```nginx
root /www/wwwroot/qilipaper-vanglam-static;

location / {
    try_files $uri $uri/ /index.html;
}
```

**配置检查**：

```bash
/www/server/nginx/sbin/nginx -t
```

**结果**：

```text
nginx configuration test is successful
```

**重载 Nginx**：

```bash
/etc/init.d/nginx reload
```

**结果**：Nginx 重载成功。

---

### 14. 修复 `/vanglam` 路由 403 问题

**问题**：

新静态包中存在 `/vanglam` 静态资源目录。Nginx 使用：

```nginx
try_files $uri $uri/ /index.html;
```

时，访问：

```text
https://qilipaper.com/vanglam
```

会被当作目录访问，返回 403。

**修复前备份**：

```text
/www/backup/qilipaper/node_paper_main.conf.fix-vanglam-route-20260606-220624.bak
```

**修复配置**：

```nginx
location / {
    try_files $uri /index.html;
}
```

**配置检查**：

```bash
/www/server/nginx/sbin/nginx -t
```

**结果**：配置测试通过。

**重载 Nginx**：

```bash
/etc/init.d/nginx reload
```

**结果**：`/vanglam` 路由恢复正常。

---

### 15. 停止旧 Next.js 服务

**目的**：新静态站已经接管正式域名，旧 Next.js 服务不再需要继续占用内存和 3000 端口。

**停止前查询**：

```bash
ps -eo pid,user,cmd | grep -E "npm start|next-server" | grep -v grep
```

**停止命令摘要**：

```bash
pkill -u www -f "next-server"
pkill -u www -f "npm start"
```

**停止后查询**：

```bash
ps -eo pid,user,cmd | grep -E "npm start|next-server" | grep -v grep
ss -tulpn | grep 3000
```

**结果**：

```text
npm start 已停止
next-server 已停止
3000 端口已不再承载旧站
```

---

### 16. 上线验证

**目的**：确认正式域名已访问新前端。

**命令摘要**：

```bash
curl -k -L -s -o /dev/null -w "%{http_code}" https://qilipaper.com/
curl -k -L -s -o /dev/null -w "%{http_code}" https://qilipaper.com/vanglam
curl -k -L -s -o /dev/null -w "%{http_code}" https://qilipaper.com/vanglam/color-system
curl -k -L -s -o /dev/null -w "%{http_code}" https://qilipaper.com/vanglam/collections
curl -k -L -s -o /dev/null -w "%{http_code}" https://qilipaper.com/vanglam/request-sample-kit
curl -k -L -s -o /dev/null -w "%{http_code}" https://qilipaper.com/vanglam/hero-paper.png
curl -k -L -s -o /dev/null -w "%{http_code}" https://qilipaper.com/assets/index-BIY00_Wd.js
```

**验证结果**：

```text
200 https://qilipaper.com/
200 https://qilipaper.com/vanglam
200 https://qilipaper.com/vanglam/color-system
200 https://qilipaper.com/vanglam/collections
200 https://qilipaper.com/vanglam/request-sample-kit
200 https://qilipaper.com/vanglam/hero-paper.png
200 https://qilipaper.com/assets/index-BIY00_Wd.js
```

---

### 17. 安全处理复查

**目的**：确认异常文件已经不在旧站目录、没有异常进程，新站仍然正常。

**命令摘要**：

```bash
ls -lah /www/wwwroot/paper-main/.pwned /www/wwwroot/paper-main/sshddm
ps -eo pid,user,cmd | grep -E "sshddm|/www/wwwroot/paper-main/.pwned|/www/wwwroot/paper-main/sshddm"
find /www/backup/qilipaper/security-20260606-220341 -maxdepth 1 -type f -printf "%f %s bytes %m %u:%g\n"
grep -n "server_name\|qilipaper-vanglam-static\|try_files\|proxy_pass" /www/server/panel/vhost/nginx/node_paper_main.conf
curl -k -L -s -o /dev/null -w "%{http_code}" https://qilipaper.com/
```

**复查结果**：

```text
旧站目录中的 .pwned 不存在
旧站目录中的 sshddm 不存在
未发现 sshddm 相关运行进程
隔离文件权限为 600
Nginx 当前指向 /www/wwwroot/qilipaper-vanglam-static
https://qilipaper.com/ 返回 200
https://qilipaper.com/vanglam 返回 200
https://qilipaper.com/vanglam/color-system 返回 200
```

---

### 18. 清理 VPS 垃圾文件和无效数据

**目的**：按照项目负责人要求，让 VPS 保持简洁，只保留旧网站数据、新网站数据、备份和必要运行环境。

**处理原则**：

1. 保留旧网站源码目录。
2. 保留旧网站上传数据。
3. 保留旧网站源码备份。
4. 保留新前端静态站目录。
5. 保留 Nginx、MySQL、SSL 和站点配置。
6. 删除或移动可再生、无效、临时、空白和缓存类文件。

**清理前主要占用**：

```text
/www/wwwroot/paper-main/node_modules 约 1.1G
/www/wwwroot/paper-main/.next 约 610M
/root/.cache 约 47M
/www/wwwlogs/paper_main.log 约 167M
/www/wwwroot/paper-main.zip 约 89M
```

**第一次清理命令状态**：

```text
/www/backup/qilipaper/cleanup-20260606-221920.log
```

第一次清理在停止宝塔面板进程时中断。原因是停止命令使用了进程匹配，SSH 执行脚本本身也被匹配到，导致当前 SSH 命令提前结束。

第一次清理已完成的动作：

1. 检查磁盘状态。
2. 检查旧站目录大小。
3. 停止空 PM2 守护进程。
4. 尝试停止 FTP。
5. 尝试停止宝塔面板。

第一次清理未执行的动作：

1. 旧 `node_modules` 删除。
2. 旧 `.next` 删除。
3. 缓存清理。
4. 日志轮转。

**第二次继续清理记录**：

```text
/www/backup/qilipaper/cleanup-continue-20260606-222026.log
```

第二次清理执行动作：

```bash
rm -rf /www/wwwroot/paper-main/node_modules
rm -rf /www/wwwroot/paper-main/.next
rm -rf /root/.cache/*
rm -rf /root/.npm/_cacache
rm -rf /www/.Recycle_bin/*
gzip -c /www/wwwlogs/paper_main.log > /www/backup/qilipaper/paper_main.log.20260606-222026.gz
: > /www/wwwlogs/paper_main.log
rm -f /www/wwwroot/paper-main/5.sh
```

**删除说明**：

| 路径 | 处理 | 原因 |
| --- | --- | --- |
| `/www/wwwroot/paper-main/node_modules` | 删除 | 可通过 `npm install` 重新生成 |
| `/www/wwwroot/paper-main/.next` | 删除 | 可通过 `npm run build` 重新生成 |
| `/root/.cache/*` | 删除 | root 用户缓存 |
| `/root/.npm/_cacache` | 删除 | npm 下载缓存 |
| `/www/.Recycle_bin/*` | 清空 | 宝塔回收站内容 |
| `/www/wwwlogs/paper_main.log` | 压缩备份后截断 | 旧访问日志过大 |
| `/www/wwwroot/paper-main/5.sh` | 删除 | 0 字节空文件 |

**日志备份**：

```text
/www/backup/qilipaper/paper_main.log.20260606-222026.gz
```

**清理结果**：

```text
根磁盘已用：18G -> 16G
/www/wwwroot/paper-main：约 1.8G -> 106M
/www/wwwroot：约 1.9G -> 195M
```

---

### 19. 停用不需要的服务并收紧本机监听

**目的**：减少海外 VPS 被扫描和攻击的暴露面。

**处理内容**：

1. 停止并禁用宝塔面板服务。
2. 停止并禁用 FTP 服务。
3. 将 MySQL 从公网监听改为仅本机监听。
4. 保留 Nginx 80 / 443。
5. 保留 SSH 22，后续建议在阿里云安全组限制来源 IP。

**命令日志**：

```text
/www/backup/qilipaper/security-tighten-20260606-222158.log
```

**MySQL 配置备份**：

```text
/www/backup/qilipaper/my.cnf.before-local-bind-20260606-222158.bak
```

**MySQL 修改内容**：

```text
bind-address = 127.0.0.1
```

**服务处理结果**：

| 服务 / 端口 | 处理前 | 处理后 |
| --- | --- | --- |
| 宝塔面板 8888 | 公网监听 | 已停止，已禁用自启 |
| FTP 21 | 曾运行 | 已停止，已禁用自启 |
| MySQL 3306 | 公网监听 | 仅监听 `127.0.0.1:3306` |
| Nginx 80 | 运行 | 保留 |
| Nginx 443 | 运行 | 保留 |
| SSH 22 | 运行 | 保留 |

**复查结果**：

```text
80  继续监听
443 继续监听
22  继续监听
3306 仅监听 127.0.0.1
8888 不再监听
21 不再监听
```

**备注**：

云厂商安全组不在 VPS 系统内部，后续仍建议在阿里云控制台继续限制：

```text
22 只允许开发者固定 IP
80 允许公网
443 允许公网
8888 不开放
3306 不开放
21 不开放
```

---

### 20. 整理 `/www/wwwroot` 目录

**目的**：让网站根目录保持简洁，只保留旧站目录和新站目录。

**命令日志**：

```text
/www/backup/qilipaper/wwwroot-tidy-20260606-222903.log
```

**执行动作**：

1. 将旧上传压缩包从网站根目录移动到备份区。
2. 删除空的默认站点目录。

**移动文件**：

```text
/www/wwwroot/paper-main.zip
```

移动到：

```text
/www/backup/qilipaper/paper-main-original-upload-20250811.zip
```

**删除目录**：

```text
/www/wwwroot/default
```

删除原因：

```text
该目录为空，且不是当前业务站点目录。
```

**整理后 `/www/wwwroot` 内容**：

```text
/www/wwwroot/paper-main
/www/wwwroot/qilipaper-vanglam-static
```

---

### 21. 清理后最终复查

**目的**：确认清理后网站正常、可疑文件不存在、端口暴露减少。

**最终磁盘状态**：

```text
根磁盘：40G
已用：16G
可用：22G
使用率：43%
```

**最终 `/www/wwwroot` 状态**：

```text
/www/wwwroot/paper-main                  约 106M
/www/wwwroot/qilipaper-vanglam-static   约 1.4M
/www/wwwroot 总计                       约 107M
```

**最终监听状态**：

```text
80    Nginx 公网监听
443   Nginx 公网监听
22    SSH 公网监听
3306  MySQL 仅监听 127.0.0.1
21    不监听
8888  不监听
3000  不监听
```

**最终进程状态**：

```text
Nginx 运行中
MySQL 运行中
宝塔面板未运行
FTP 未运行
PM2 未运行
旧 Next.js 未运行
```

**新站验证**：

```text
200 https://qilipaper.com/
200 https://qilipaper.com/vanglam
200 https://qilipaper.com/vanglam/color-system
```

**可疑文件复查**：

```text
/www/wwwroot/paper-main/.pwned 不存在
/www/wwwroot/paper-main/sshddm 不存在
未发现 sshddm 相关运行进程
```

---

### 22. 清理 SSH 中断后遗留的审计进程

**目的**：前一次远程安全收紧命令因 SSH 会话超时，在服务器上留下了一个只负责写日志的审计脚本进程。该进程不属于网站运行所需服务，因此进行清理。

**处理内容**：

```text
发现遗留进程：security-tighten 审计脚本 bash 进程
发现遗留子进程：tee 日志写入进程
执行处理：终止该遗留 bash 进程和 tee 子进程
保留日志：/www/backup/qilipaper/cleanup-finish-20260606-223407.log
```

**复查结果**：

```text
遗留 bash 进程已不存在
遗留 tee 子进程已不存在
80、443、22、3306 监听状态正常
8888、3000、21 未监听
https://qilipaper.com/ 返回 200
https://qilipaper.com/vanglam 返回 200
https://qilipaper.com/vanglam/color-system 返回 200
```

---

### 23. 删除公开网站中的后台开发文档入口

**时间**：二零二六年六月六日
**目的**：公开官网只展示客户需要看到的官网内容，不再展示后台开发文档、开发费用、开发范围、思维导图、服务器日志等内部资料。后续这些内容只保存在 Markdown 文档中。

**处理前公开痕迹**：

```text
导航右上角存在“后台开发文档”入口
前端路由存在 /vanglam/backend-plan
index.html 描述中包含“后台开发进度文档页”
公开 JS 中包含后台建设开发文档内容
```

**本地源码处理**：

```text
删除导航中的“后台开发文档”按钮
删除 /vanglam/backend-plan 页面路由
删除前端中的后台开发文档页面组件
删除源码包中的后台需求文档副本
修改 index.html 描述为“齐力纸业 VANGLAM 梵澜官网展示页”
重新执行 npm run build
```

**新部署包**：

```text
本地部署包：D:\webcode\Retro-camera-main\.codex-tmp\qilipaper-vanglam-dist-public-clean-20260606.zip
服务器部署包：/www/backup/qilipaper/qilipaper-vanglam-dist-public-clean-20260606.zip
本地干净源码包：D:\webcode\Retro-camera-main\.codex-tmp\qilipaper-vanglam-source-public-clean-20260606.zip
服务器干净源码包：/www/backup/qilipaper/qilipaper-vanglam-source-public-clean-20260606.zip
```

**服务器处理**：

```text
备份当前公开静态目录：
/www/backup/qilipaper/qilipaper-vanglam-static-before-public-clean-20260606-225157.tar.gz

替换公开静态目录：
/www/wwwroot/qilipaper-vanglam-static

删除公开目录中旧的 JS 文件，避免旧后台文档内容仍可通过旧资源地址访问。
```

**Nginx 处理**：

```text
备份 Nginx 配置：
/www/backup/qilipaper/node_paper_main.conf.before-remove-backend-plan-20260606-225257.bak
/www/backup/qilipaper/node_paper_main.conf.before-assets-no-spa-fallback-20260606-230236.bak

新增旧地址跳转：
/vanglam/backend-plan 302 跳转到 /vanglam

新增静态资源规则：
/assets/ 下不存在的旧资源返回 404，不再回退到 index.html

操作日志：
/www/backup/qilipaper/nginx-remove-backend-plan-route-20260606-225257.log
/www/backup/qilipaper/nginx-assets-no-spa-fallback-20260606-230236.log
```

**复查结果**：

```text
公开静态目录中未发现“后台开发文档”
公开静态目录中未发现“齐力纸业梵澜官网后台建设开发文档”
公开静态目录中未发现“人民币三万元”
公开静态目录中未发现“七大主题思维导图”
https://qilipaper.com/ 返回 200
https://qilipaper.com/vanglam 返回 200
https://qilipaper.com/vanglam/backend-plan 跳转到 https://qilipaper.com/vanglam
https://qilipaper.com/assets/index-BJvF-hUs.js 返回 200
https://qilipaper.com/assets/index-BIY00_Wd.js 返回 404
```

**本次操作日志**：

```text
/www/backup/qilipaper/remove-public-backend-doc-20260606-225157.log
/www/backup/qilipaper/nginx-remove-backend-plan-route-20260606-225257.log
/www/backup/qilipaper/nginx-assets-no-spa-fallback-20260606-230236.log
/www/backup/qilipaper/cleanup-hidden-cr-filenames-20260606-231021.log
```

---

### 24. 清理服务器日志文件名中的隐藏回车符

**时间**：二零二六年六月六日
**目的**：本机 PowerShell 通过 SSH 执行多行 Linux 命令时，少数新生成的日志文件名末尾带了隐藏回车符。该问题不影响网站运行，但会导致后续按精确路径查找日志时失败，因此进行文件名清洗。

**处理内容**：

```text
清理以下文件名末尾隐藏回车符：
/www/backup/qilipaper/nginx-remove-backend-plan-route-20260606-225257.log
/www/backup/qilipaper/cleanup-finish-20260606-223407.log
/www/backup/qilipaper/nginx-assets-no-spa-fallback-20260606-230236.log
/www/backup/qilipaper/remove-public-backend-doc-20260606-225157.log
```

**复查结果**：

```text
以上日志文件均可通过正常路径精确访问
清理记录：/www/backup/qilipaper/cleanup-hidden-cr-filenames-20260606-231021.log
```

---

### 25. 修改公开网站页脚版权年份

**时间**：二零二六年六月六日
**目的**：按客户要求，将公开官网页脚版权年份从二零二四年改为二零二六年，中英文同步修改。

**修改内容**：

```text
英文：
© 2024 QiLi Paper. All rights reserved.
改为：
© 2026 QiLi Paper. All rights reserved.

中文：
© 2024 齐力纸业。保留所有权利。
改为：
© 2026 齐力纸业。保留所有权利。
```

**本地处理**：

```text
修改源码文件：
D:\webcode\Retro-camera-main\.codex-tmp\qilipaper-vanglam-source\components\vanglam\VanglamLanguage.tsx

重新执行构建：
npm run build
```

**新部署包**：

```text
本地部署包：D:\webcode\Retro-camera-main\.codex-tmp\qilipaper-vanglam-dist-copyright-2026-20260606.zip
服务器部署包：/www/backup/qilipaper/qilipaper-vanglam-dist-copyright-2026-20260606.zip
本地源码包：D:\webcode\Retro-camera-main\.codex-tmp\qilipaper-vanglam-source-copyright-2026-20260606.zip
服务器源码包：/www/backup/qilipaper/qilipaper-vanglam-source-copyright-2026-20260606.zip
```

**服务器处理**：

```text
备份当前公开静态目录：
/www/backup/qilipaper/qilipaper-vanglam-static-before-copyright-2026-20260606-231839.tar.gz

替换公开静态目录：
/www/wwwroot/qilipaper-vanglam-static
```

**复查结果**：

```text
公开静态目录中未发现 © 2024
公开静态目录中未发现 2024 QiLi
公开静态目录中未发现 2024 齐力
公开静态目录中已包含 © 2026 QiLi Paper. All rights reserved.
公开静态目录中已包含 © 2026 齐力纸业。保留所有权利。
https://qilipaper.com/ 返回 200
https://qilipaper.com/vanglam 返回 200
https://qilipaper.com/assets/index-DLbyXMLO.js 返回 200
https://qilipaper.com/assets/index-BJvF-hUs.js 返回 404
```

**本次操作日志**：

```text
/www/backup/qilipaper/update-copyright-2026-20260606-231839.log
```

---

### 26. 最终安全复查与服务停用确认

**时间**：二零二六年六月六日 23:29
**目的**：按项目负责人要求，确认“安全组收紧、宝塔停用、不直接删除宝塔”的执行边界，同时复查服务器最后的安全问题是否还存在。

**处理原则**：

1. 宝塔面板只停用，不卸载。
2. FTP 不作为当前业务需要，保持停用。
3. MySQL 不对公网开放，只允许本机连接。
4. 旧网站和新网站数据继续保留。
5. 不执行会导致客户无法登录的 SSH 配置修改。
6. 阿里云安全组属于云控制台层，不在 VPS 系统内部直接修改。

**命令摘要**：

```bash
/etc/init.d/bt stop
systemctl stop bt
systemctl disable bt
systemctl stop pure-ftpd pureftpd
systemctl disable pure-ftpd pureftpd
ss -lntup
systemctl is-active / is-enabled
lastb -a
```

**操作日志**：

```text
/www/backup/qilipaper/final-security-hardening-20260606-232922.log
```

**复查结果**：

```text
宝塔面板：inactive / disabled
FTP：inactive / disabled
Nginx：active / enabled
MySQL：active / enabled，仅监听 127.0.0.1:3306
SSH：active / enabled，监听 22
旧 Next.js：未监听 3000
宝塔面板 8888：未监听
```

**发现的剩余风险**：

```text
/etc/ssh/sshd_config 中仍存在：
PermitRootLogin yes
PasswordAuthentication yes
```

同时 `lastb` 仍能看到海外 IP 对 SSH 的失败登录尝试。说明服务器还在被公网扫描，根因是 `22` 端口仍对公网开放，并且 SSH 仍允许密码登录。

**未直接修改 SSH 的原因**：

关闭 SSH 密码登录前，必须确认客户、开发方和后续维护人员都已经配置好 SSH 公钥。否则一旦关闭密码登录，客户可能无法再通过 Tabby 或其他终端登录服务器。

---

### 27. 关闭 Nginx 默认 phpMyAdmin 888 入口

**时间**：二零二六年六月六日 23:31
**目的**：最终复查时发现 Nginx 主配置中仍存在默认 phpMyAdmin `listen 888` 入口。该端口外网当时不可达，但服务器内部仍在监听，因此关闭该默认入口，让 VPS 本身也更干净。

**处理前发现**：

```text
/www/server/nginx/conf/nginx.conf: listen 888;
server_name phpmyadmin;
root /www/server/phpmyadmin;
```

**配置备份**：

```text
/www/backup/qilipaper/nginx.conf-before-remove-phpmyadmin-888-20260606-233145.bak
```

**命令摘要**：

```bash
cp -a /www/server/nginx/conf/nginx.conf /www/backup/qilipaper/nginx.conf-before-remove-phpmyadmin-888-20260606-233145.bak
移除 nginx.conf 中默认 phpMyAdmin server 块
/www/server/nginx/sbin/nginx -t -c /www/server/nginx/conf/nginx.conf
/www/server/nginx/sbin/nginx -s reload -c /www/server/nginx/conf/nginx.conf
```

**配置测试结果**：

```text
nginx configuration test is successful
```

**复查结果**：

```text
888 不再监听
8888 不监听
21 不监听
3000 不监听
3306 仅监听 127.0.0.1
80 / 443 正常监听
https://qilipaper.com/ 返回 200
https://qilipaper.com/vanglam 返回 200
```

**操作日志**：

```text
/www/backup/qilipaper/close-default-ports-and-remove-quarantine-20260606-233145.log
```

---

### 28. 删除隔离目录中的可疑文件副本

**时间**：二零二六年六月六日 23:31
**目的**：旧站目录中的异常文件此前已经删除，但隔离目录中仍保留了副本。客户要求 VPS 尽量干净，因此在记录哈希后删除隔离副本，只保留审计日志。

**处理文件**：

```text
/www/backup/qilipaper/security-20260606-220341/.pwned
/www/backup/qilipaper/security-20260606-220341/sshddm
```

**删除前记录 SHA256**：

```text
.pwned  1060092d1ce0ae5ca5ac11bc1d078c5fa9e263f3fb6c736293a5dbb018e59258
sshddm  d04380d79168f863515bb6c625de913a2f62e8160d6913c57b83a6cb6b36c5ed
```

**命令摘要**：

```bash
sha256sum 隔离文件
rm -f 隔离文件
find /www/wwwroot /www/backup/qilipaper -maxdepth 5 \
  -iname '*pwned*' -o -iname '*sshddm*' -o -iname '*xmrig*' -o -iname '*kinsing*' -o -iname '*kdevtmpfsi*'
```

**复查结果**：

```text
旧站目录没有 .pwned
旧站目录没有 sshddm
隔离目录中的 .pwned 已删除
隔离目录中的 sshddm 已删除
服务器内未再扫到上述可疑命名文件
```

**保留记录**：

```text
/www/backup/qilipaper/close-default-ports-and-remove-quarantine-20260606-233145.log
```

---

### 29. 阿里云安全组与 SSH 当前状态

**时间**：二零二六年六月六日 23:32-23:33
**目的**：确认安全组能否由 VPS 内部直接收紧，并说明当前 SSH 风险是否完全处理。

**已确认**：

```text
服务器有 aliyun CLI
服务器实例元数据中没有可用 RAM 角色凭证
VPS 内部无法直接调用阿里云 API 修改安全组
```

**外部端口复查结果**：

```text
21    外部不可达
22    外部可达
80    外部可达
443   外部可达
888   外部不可达
8888  外部不可达
3306  外部不可达
3000  外部不可达
```

**当前结论**：

服务器系统内部已经完成当前可安全执行的收紧动作。最后仍未完全解决的是 SSH 暴力扫描风险，因为 `22` 仍对公网可达，并且 SSH 仍允许 root 密码登录。

**下一步需要在阿里云控制台处理**：

```text
80    允许公网访问
443   允许公网访问
22    只允许开发者固定 IP 访问
8888  不开放
3306  不开放
21    不开放
3000  不开放
```

**下一步需要确认后再处理**：

```text
确认客户、开发方和维护人员都能使用 SSH 公钥登录
然后再把 PasswordAuthentication 改为 no
再把 PermitRootLogin 改为 prohibit-password
重载 sshd 前必须先保留一个已登录 SSH 窗口用于回滚
```

---

## 四、当前服务器上线后状态

当前线上状态：

```text
qilipaper.com 已运行新前端静态站
旧 Next.js 服务已停止
旧网站源码已备份
异常文件已从旧目录删除
异常文件隔离副本已记录 SHA256 后删除
旧站 node_modules 已删除
旧站 .next 构建产物已删除
宝塔面板已停止并禁用自启
FTP 已停止并禁用自启
MySQL 已限制为本机监听
Nginx 默认 phpMyAdmin 888 入口已关闭
公开官网已删除后台开发文档入口
后台开发记录改为只保留在 Markdown 文档中
公开官网页脚版权年份已更新为 2026
```

当前 Nginx 关键配置：

```nginx
server_name qilipaper.com;
root /www/wwwroot/qilipaper-vanglam-static;

location = /vanglam/backend-plan {
    return 302 /vanglam;
}

location ^~ /assets/ {
    try_files $uri =404;
}

location / {
    try_files $uri /index.html;
}
```

当前新站目录：

```text
/www/wwwroot/qilipaper-vanglam-static
```

当前旧站目录：

```text
/www/wwwroot/paper-main
```

旧站目录目前只保留源码、配置和上传数据，不再保留 `node_modules` 和 `.next` 构建产物。

当前旧站备份：

```text
/www/backup/qilipaper/paper-main-source-20260606-220341.tar.gz
```

原异常文件隔离目录：

```text
/www/backup/qilipaper/security-20260606-220341
```

当前状态：

```text
隔离目录中的 .pwned 已记录 SHA256 后删除
隔离目录中的 sshddm 已记录 SHA256 后删除
可疑文件哈希和删除记录保留在操作日志中
```

当前清理日志：

```text
/www/backup/qilipaper/cleanup-20260606-221920.log
/www/backup/qilipaper/cleanup-continue-20260606-222026.log
/www/backup/qilipaper/security-tighten-20260606-222158.log
/www/backup/qilipaper/wwwroot-tidy-20260606-222903.log
/www/backup/qilipaper/cleanup-finish-20260606-223407.log
/www/backup/qilipaper/remove-public-backend-doc-20260606-225157.log
/www/backup/qilipaper/nginx-remove-backend-plan-route-20260606-225257.log
/www/backup/qilipaper/nginx-assets-no-spa-fallback-20260606-230236.log
/www/backup/qilipaper/cleanup-hidden-cr-filenames-20260606-231021.log
/www/backup/qilipaper/update-copyright-2026-20260606-231839.log
/www/backup/qilipaper/final-security-hardening-20260606-232922.log
/www/backup/qilipaper/close-default-ports-and-remove-quarantine-20260606-233145.log
```

---

## 五、后续注意事项

1. 不要删除 `/www/backup/qilipaper`，其中包含旧站备份、Nginx 备份、操作日志和异常文件哈希记录。
2. 后续如果要彻底清理服务器，建议在备份必要源码和数据库后重装 VPS。
3. 后续正式后台上线前，应在阿里云控制台收紧安全组，尤其是 SSH `22` 只允许固定 IP。
4. 关闭 SSH 密码登录前，必须先确认客户和开发方都能用 SSH 公钥登录。
5. 如果客户要求恢复旧站，可根据 Nginx 配置备份和旧站源码备份回滚。
6. 如果继续开发后台，应新建干净项目和数据库结构，不建议复用旧 Next.js 后台作为长期后台。

---

## 六、二零二六年六月七日 V1.1 前端部署审计

### 1. 操作目标

将客户新 PDF 对应的 V1.1 前端确认版部署到客户阿里云 VPS，让客户可以通过正式域名查看新增页面。

本次只部署静态前端，不部署后台、API、数据库和小程序。

### 2. SSH 连通性确认

本地使用 SSH 公钥登录 VPS，执行：

```text
hostname
whoami
pwd
date
```

确认结果：

```text
登录用户：root
工作目录：/root
服务器时间：2026-06-07 17:07:49 CST
```

### 3. 本地构建与打包

本地源码目录：

```text
D:\webcode\Retro-camera-main\.codex-tmp\qilipaper-vanglam-source
```

执行：

```text
npm run verify:v11
npm run build
```

结果：通过。

本地部署包：

```text
D:\webcode\Retro-camera-main\.codex-tmp\qilipaper-vanglam-dist-v11-20260607-170841.tar.gz
```

### 4. 上传部署包

服务器部署包路径：

```text
/www/backup/qilipaper/qilipaper-vanglam-dist-v11-20260607-170841.tar.gz
```

上传方式：

```text
scp 上传到 /www/backup/qilipaper/
```

### 5. 服务器备份

部署前备份当前线上静态目录：

```text
/www/backup/qilipaper/qilipaper-vanglam-static-before-v11-20260607-170841.tar.gz
```

备份来源：

```text
/www/wwwroot/qilipaper-vanglam-static
```

### 6. 服务器部署动作

服务器部署目标目录：

```text
/www/wwwroot/qilipaper-vanglam-static
```

执行动作：

1. 新建临时目录。
2. 解压 V1.1 前端部署包。
3. 检查 `index.html` 和 `assets` 是否存在。
4. 设置目录权限和文件权限。
5. 将旧线上目录临时改名。
6. 将新目录替换为正式线上目录。
7. 执行 `nginx -t`。
8. 执行 `nginx -s reload`。
9. 验证访问状态。
10. 确认部署成功后删除临时旧目录。

服务器部署日志：

```text
/www/backup/qilipaper/deploy-vanglam-v11-20260607-170841.log
```

### 7. 服务器端验证

服务器端执行访问验证：

```text
curl -I https://qilipaper.com/vanglam
curl -I https://qilipaper.com/vanglam/material-selector
```

结果：

```text
HTTP/2 200
```

Nginx 配置检查：

```text
nginx -t
```

结果：

```text
syntax is ok
test is successful
```

### 8. 本地线上浏览器验证

部署后本地执行线上浏览器测试：

```text
VANGLAM_BASE_URL=https://qilipaper.com npm run test:v11
```

结果：

```text
14 passed
```

测试覆盖：

1. `/vanglam`
2. `/vanglam/artlabel-lab`
3. `/vanglam/material-selector`
4. `/vanglam/downloads`
5. `/vanglam/sample-system`
6. `/vanglam/atelier/delivery-system`
7. `/vanglam/applications/wine-spirits-labels`

测试设备：

1. 桌面端 1440px。
2. 移动端 390px。

### 9. 当前结论

V1.1 前端确认版已部署到客户 VPS。

当前线上仍是静态前端站点，后台、API、数据库、小程序均未部署。

---

## 2026 年 6 月 7 日 17:47 - 后台/API/数据库拆分文档同步记录

### 1. 本次操作目的

V1.1 前端确认版已经部署到客户 VPS。

本次继续开发前，先把后台、数据库和 API 的拆分文档同步到客户 VPS 的备份目录，作为后续开发和部署依据。

### 2. 本次不影响公网网站

本次操作只同步 Markdown 文档包。

没有修改以下内容：

1. 没有修改 `/www/wwwroot/qilipaper-vanglam-static`。
2. 没有修改 Nginx 配置。
3. 没有修改数据库。
4. 没有修改 SSL 证书。
5. 没有修改旧网站源码。
6. 没有重启业务服务。

### 3. 本次准备同步的文档

本次文档包包含：

```text
docs/superpowers/specs/齐力纸业V1.1后台数据库API字段拆分说明.md
docs/superpowers/plans/2026-06-07-qilipaper-backend-api-foundation.md
docs/superpowers/specs/齐力纸业项目开发日志.md
docs/superpowers/specs/齐力纸业开发方向与需求变更记录.md
docs/superpowers/specs/齐力纸业服务器操作审计记录.md
```

### 4. 服务器保存位置

文档包已保存到：

```text
/www/backup/qilipaper/project-docs/qilipaper-docs-backend-api-mapping-20260607-174756.tar.gz
```

该目录属于备份目录，不属于公网网站目录。

### 5. 上传验证结果

服务器端已验证文件存在：

```text
/www/backup/qilipaper/project-docs/qilipaper-docs-backend-api-mapping-20260607-174756.tar.gz
```

文档包内容已验证，包含：

```text
docs/superpowers/specs/齐力纸业V1.1后台数据库API字段拆分说明.md
docs/superpowers/specs/齐力纸业开发方向与需求变更记录.md
docs/superpowers/specs/齐力纸业服务器操作审计记录.md
docs/superpowers/specs/齐力纸业项目开发日志.md
docs/superpowers/plans/2026-06-07-qilipaper-backend-api-foundation.md
```

### 6. 当前结论

本次同步只用于项目记录和后续开发依据。

公网客户网站入口仍保持：

```text
https://qilipaper.com/vanglam
```

---

## 2026 年 6 月 7 日 18:04 - 后台/API 地基源码备份记录

### 1. 本次操作目的

本次在本地新建齐力纸业后续正式后台/API/数据库项目：

```text
D:\webcode\qilipaper-platform
```

为了满足“每次完成开发后同步到客户 VPS”的规则，本次把该项目第一版地基源码打包上传到客户 VPS 的备份目录。

### 2. 本次没有影响公网服务

本次没有执行以下动作：

1. 没有替换 `/www/wwwroot/qilipaper-vanglam-static`。
2. 没有修改 Nginx 配置。
3. 没有启动后台服务。
4. 没有连接正式数据库。
5. 没有修改旧网站源码。
6. 没有修改 SSL 证书。

公网网站仍保持：

```text
https://qilipaper.com/vanglam
```

---

## 2026 年 6 月 8 日 10:39 - VANGLAM 首页首屏图片动效补丁部署

### 1. 本次操作目的

客户希望首页首屏图片增加炫酷、引人注意的动画效果。

本次只在当前静态前端中追加首页首屏动效补丁，不修改 Nginx、后台服务、API 或数据库。

### 2. 本机 SSH 工具确认

本机 PATH 中未直接找到 `ssh` 和 `scp`，但 Git for Windows 已提供可用工具：

```text
C:\Program Files\Git\usr\bin\ssh.exe
C:\Program Files\Git\usr\bin\scp.exe
```

已用该工具验证可以免密连接 VPS。

### 3. 上线前备份

服务器备份目录：

```text
/www/backup/qilipaper/hero-motion-20260608-103921
```

已备份上线前 `index.html`：

```text
/www/backup/qilipaper/hero-motion-20260608-103921/index.before-hero-motion.html
```

### 4. 本次上传文件

上传到备份目录：

```text
/www/backup/qilipaper/hero-motion-20260608-103921/index.html
/www/backup/qilipaper/hero-motion-20260608-103921/vanglam-hero-motion-20260608.css
/www/backup/qilipaper/hero-motion-20260608-103921/vanglam-hero-motion-20260608.js
```

已复制到正式静态目录：

```text
/www/wwwroot/qilipaper-vanglam-static/index.html
/www/wwwroot/qilipaper-vanglam-static/assets/vanglam-hero-motion-20260608.css
/www/wwwroot/qilipaper-vanglam-static/assets/vanglam-hero-motion-20260608.js
```

### 5. 文件校验

上传后服务器端 SHA256：

```text
4eb376606495793006110ba05757b338ba1c122354636398a8f8b2f9a2e1502b  index.html
d2681df31c589fb26d8f9b8d90c9341836712186e08e51372d9ebbdfef2540ff  vanglam-hero-motion-20260608.css
dc31dba5671dee3d5b955565b9edffdbc65c77df7ba2c10041bfdd37feb307ae  vanglam-hero-motion-20260608.js
```

正式目录文件权限：

```text
www:www
644
```

### 6. HTTP 与浏览器验证

HTTP 验证：

```text
https://qilipaper.com/vanglam                                  200
https://qilipaper.com/assets/vanglam-hero-motion-20260608.css  200
https://qilipaper.com/assets/vanglam-hero-motion-20260608.js   200
```

浏览器验证：

```text
VANGLAM_BASE_URL=https://qilipaper.com node verify-vanglam-motion.cjs
```

结果：

```text
通过
```

验证内容：

1. 首屏动效补丁成功挂载。
2. 图片揭幕、纸面呼吸、金色扫光和鼠标视差生效。
3. 桌面和移动端首屏无文案图片重叠。
4. 减弱动画模式下动画关闭。
5. 浏览器控制台无错误。

### 7. 影响范围

本次没有修改：

1. Nginx 配置。
2. 后台服务。
3. API 服务。
4. 数据库。
5. 旧站目录。

如需回退，可恢复备份目录中的 `index.before-hero-motion.html`，并删除正式目录中的两个 `vanglam-hero-motion-20260608` 补丁文件。

---

## 2026 年 6 月 8 日 11:09 - 首页三款标志色图片替换

### 1. 本次操作目的

客户反馈首页三款标志色图片颜色不够真实，要求根据客户颜色体系工作簿中的色值调整。

本次只替换首页三张标志色图片，不修改 Nginx、主 bundle、后台、API 或数据库。

### 2. 本次颜色依据

客户工作簿中品牌三色：

```text
D06 石绿 / Mineral Green / #2A826A
B03 秋香 / Aged Citron / #9E8F2E
E04 钴釉蓝 / Cobalt Glaze / #244FA3
```

### 3. 上线前备份

服务器备份目录：

```text
/www/backup/qilipaper/signature-colors-20260608-110923
```

上线前旧图备份：

```text
signature-green.before.png
signature-citron.before.png
signature-blue.before.png
```

旧图 SHA256：

```text
8bf23fd057b8768f3463da5eb27c03fceb5b85ec3ef8aa3e326e40e463424dd8  signature-green.before.png
8192692e2f696b226ccd692109a8a7ed54021672a9ef56c07732b40768080196  signature-citron.before.png
fa59f112eca3e47f2a7adbb98f3bf48af51b67fd06842a76375f34ee338bd481  signature-blue.before.png
```

### 4. 本次上传文件

上传到备份目录：

```text
/www/backup/qilipaper/signature-colors-20260608-110923/signature-green.png
/www/backup/qilipaper/signature-colors-20260608-110923/signature-citron.png
/www/backup/qilipaper/signature-colors-20260608-110923/signature-blue.png
```

复制到正式静态目录：

```text
/www/wwwroot/qilipaper-vanglam-static/vanglam/signature-green.png
/www/wwwroot/qilipaper-vanglam-static/vanglam/signature-citron.png
/www/wwwroot/qilipaper-vanglam-static/vanglam/signature-blue.png
```

### 5. 文件校验

新图 SHA256：

```text
99abb38d5bb0eb9add3a85eabe9ba0891f0edadad379b74fc956e915861f0992  signature-green.png
e7e8aef66dcfea57160de97d02f8dcfcd8df068d4b650768615de1592ef73479  signature-citron.png
e48f4d09504476e4788afc1fc4f24150f0c738499963bfd65bf49c20284c9dee  signature-blue.png
```

正式目录权限：

```text
www:www
644
```

### 6. 浏览器验证

线上验证命令：

```text
VANGLAM_BASE_URL=https://qilipaper.com node verify-vanglam-signature-cards.cjs
```

结果：

```text
通过
```

验证内容：

1. 线上首页加载 3 张标志色图片。
2. 新图片自然尺寸为 `604 x 264`。
3. 桌面端显示正常。
4. 移动端显示正常。
5. 浏览器控制台无错误。

### 7. 影响范围

本次没有修改：

1. Nginx 配置。
2. 后台服务。
3. API 服务。
4. 数据库。
5. 旧站目录。

如需回退，可将备份目录中的 `.before.png` 三个文件复制回正式目录并改回原文件名。

### 3. 本次上传的源码包

本地源码包：

```text
D:\webcode\Retro-camera-main\.codex-tmp\qilipaper-platform-foundation-20260607-180455.tar.gz
```

服务器保存位置：

```text
/www/backup/qilipaper/source-packages/qilipaper-platform-foundation-20260607-180455.tar.gz
```

上传时已排除：

```text
node_modules
.git
dist
.next
```

### 4. 服务器端验证

服务器端已确认源码包存在：

```text
/www/backup/qilipaper/source-packages/qilipaper-platform-foundation-20260607-180455.tar.gz
```

服务器端权限已设置为：

```text
600
```

服务器端已查看压缩包内容，确认包含：

```text
qilipaper-platform/package.json
qilipaper-platform/package-lock.json
qilipaper-platform/README.md
qilipaper-platform/tsconfig.base.json
qilipaper-platform/apps
qilipaper-platform/packages
qilipaper-platform/docs
```

### 5. 本地验证结果

本地已执行：

```text
npm test
npm run typecheck
```

结果：

```text
通过
```

本地临时启动 API 后验证：

```text
GET http://127.0.0.1:4010/api/health
GET http://127.0.0.1:4010/api/routes
```

结果：

```text
health 返回 ok
共注册 9 个 API 路由
其中公开 API 4 个
后台 API 5 个
```

### 6. 当前结论

后台/API/数据库项目第一版地基已经完成本地初始化，并已同步源码包到客户 VPS 备份目录。

当前仍未在客户 VPS 上运行后台服务，后续需要等后台登录、数据库连接、内容编辑闭环完成后，再安排正式部署。

### 7. 本次最新文档包

本次更新后的开发日志、开发方向记录、后台拆分说明、后台实施计划和服务器操作审计记录会同步到：

```text
/www/backup/qilipaper/project-docs/qilipaper-docs-backend-foundation-20260607-180455.tar.gz
```

该文档包仍然只保存于备份目录，不放入公网网站目录。

---

## 2026 年 6 月 7 日 18:34 - 首页新品上线视频需求记录与源码备份

### 1. 本次操作目的

客户新增要求：

```text
首页如果用于新品上线，需要支持后台上传视频，并且官网前端和未来小程序共用同一套 API。
```

本次把该需求写入本地文档和 `qilipaper-platform` 代码契约，并同步到客户 VPS 备份目录。

### 2. 本次本地开发动作

本地已更新：

```text
D:\webcode\qilipaper-platform\packages\shared\src\v1ContentMap.ts
D:\webcode\qilipaper-platform\packages\db\prisma\schema.prisma
D:\webcode\qilipaper-platform\packages\shared\tests\v1ContentMap.test.ts
D:\webcode\qilipaper-platform\packages\db\tests\schema.test.ts
D:\webcode\qilipaper-platform\README.md
D:\webcode\qilipaper-platform\docs\开发说明.md
```

本地已更新主项目文档：

```text
docs/superpowers/specs/齐力纸业项目开发日志.md
docs/superpowers/specs/齐力纸业开发方向与需求变更记录.md
docs/superpowers/specs/齐力纸业V1.1后台数据库API字段拆分说明.md
docs/superpowers/plans/2026-06-07-qilipaper-backend-api-foundation.md
docs/superpowers/specs/齐力纸业服务器操作审计记录.md
```

### 3. 本次验证

本地已执行：

```text
npm test
npm run typecheck
```

结果：

```text
通过
```

当前测试覆盖：

```text
3 个测试文件，8 条测试通过。
```

### 4. 本次同步到 VPS 的文件

源码包：

```text
/www/backup/qilipaper/source-packages/qilipaper-platform-home-launch-video-20260607-183404.tar.gz
```

文档包：

```text
/www/backup/qilipaper/project-docs/qilipaper-docs-home-launch-video-20260607-183404.tar.gz
```

服务器端已验证以上两个文件存在，且权限已设置为：

```text
600
```

### 5. 本次没有影响公网服务

本次没有替换公网网站，没有修改 Nginx，没有启动后台服务，没有连接正式数据库。

公网网站仍保持：

```text
https://qilipaper.com/vanglam
```

---

## 2026 年 6 月 7 日 19:03 - 后台 API 第一轮闭环源码备份

### 1. 本次操作目的

本次已经开始后台 API 开发，完成第一轮后台/API 闭环。

为了满足每轮开发都同步到客户 VPS 的规则，本次将最新源码和文档打包上传到客户 VPS 备份目录。

### 2. 本次本地开发动作

本地新增：

```text
D:\webcode\qilipaper-platform\apps\api\src\store.ts
D:\webcode\qilipaper-platform\apps\api\src\http.ts
D:\webcode\qilipaper-platform\apps\api\src\routes\public.ts
D:\webcode\qilipaper-platform\apps\api\src\routes\admin.ts
D:\webcode\qilipaper-platform\apps\api\tests\apiEndpoints.test.ts
```

本地修改：

```text
D:\webcode\qilipaper-platform\apps\api\src\server.ts
```

### 3. 本次验证

本地已执行：

```text
npm test
npm run typecheck
```

结果：

```text
通过
```

当前测试覆盖：

```text
4 个测试文件，13 条测试通过。
```

本地真实 HTTP 验证：

```text
GET /api/public/home
POST /api/admin/auth/login
PUT /api/admin/pages/home
GET /api/admin/audit-logs
```

本地运行时使用临时环境变量提供测试登录能力：

```text
QILIPAPER_LOCAL_ADMIN_PASSWORD
```

源码默认不内置固定可登录密码。

结果：

```text
公开首页 API 能返回新品上线视频配置
后台登录能返回管理员信息
后台保存首页标题后，公开首页 API 能读到新标题
操作日志能返回 page.update
```

### 4. 本次同步到 VPS 的文件

源码包：

```text
/www/backup/qilipaper/source-packages/qilipaper-platform-api-loop-20260607-190307.tar.gz
```

文档包：

```text
/www/backup/qilipaper/project-docs/qilipaper-docs-api-loop-20260607-190307.tar.gz
```

服务器端已验证以上两个文件存在，且权限已设置为：

```text
600
```

### 5. 本次没有影响公网服务

本次没有替换公网网站，没有修改 Nginx，没有启动后台服务，没有连接正式数据库。

公网网站仍保持：

```text
https://qilipaper.com/vanglam
```
